services:
  udtoolapi:
    build:
      context: .
      dockerfile: UDToolAPI/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - ${TEMP:-/tmp}:/tmp
